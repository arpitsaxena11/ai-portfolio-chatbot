document.addEventListener('DOMContentLoaded',()=>{const btn=document.getElementById('ai-chat-toggle');const chat=document.getElementById('chatbot');const send=document.getElementById('sendBtn');const input=document.getElementById('userMessage');const area=document.getElementById('chatArea');btn?.addEventListener('click',()=>chat.classList.toggle('open'));send?.addEventListener('click',async()=>{const msg=input.value.trim();if(!msg)return;area.innerHTML+=`<div class="user">${msg}</div>`;input.value='';const r=await fetch('chatbot.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:msg})});const d=await r.json();area.innerHTML+=`<div class="bot">${d.reply}</div>`;area.scrollTop=area.scrollHeight;});});